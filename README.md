# Hybrid NLP + Graph Community Detection

## Overview
This project combines NLP embeddings (TF-IDF / BERT) with graph-based community detection (Louvain).

## Dataset
Sentiment140 Twitter dataset (Kaggle)

## Run
pip install -r requirements.txt
python main.py